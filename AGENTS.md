# 项目约定

- 先读 README.md、docs/requirements.md、docs/memos-contract.md。当前 0.1.2，个人自用，定制 Memos 优先，不承诺通用兼容；历史按 docs/tasks/INDEX.md 检索，只打开相关任务；开发验证方法见 docs/development-status.md。
- 使用微信客服独立后台官方 API，不混用企业微信自建应用配置。
- 公开仓库只放代码、合成测试与脱敏文档。凭据、生产地址、用户标识、真实聊天、状态库、运维备份不得提交或打印。提交前遵循 docs/security.md。
- PRIVATE、本人白名单、默认个人空间；不自动信任第一位发送者。定制版标签独立保存，不回写正文。
- 仅通过 API 操作 Memos。媒体是一篇笔记及其附件；独立微信笔记不建占位。
- 保持游标/队列事务、固定来源 ID 幂等、回执独立持久化。先保存再成功回执；发送不确定时不能盲目重发。
- 附件仅由 Memos 长期保存；扩展缓存需明确清理、容量和失败恢复。
- 检查：uv run pytest -q；uv run ruff check .。真实 API 测试仅用本机临时实例，不访问生产。
- 不把模拟测试写成真机验收，不把 API 接受回执写成手机端已读。部署前备份配置、队列和旧安装包，真实部署资料留在私有运维目录。

## 迁移到 Memos 内置模块

- 用户于 2026-09-19 选择将客服功能以 Go 内置到 `~/Code/memos`（远端 `Castor6/memos`），随同一应用、镜像和版本维护。主任务为该项目 `docs/tasks/TASK-20260919-wechat-kf-integration.md`，主方案为 `docs/wechat-kf-integration.md`。
- 本仓库 `~/Code/wechat-kf-memos` 保留 Python 0.1.2、规则和测试，供等价迁移及切换前维护使用；新的内置功能与长期 Task 写入 Memos，不再把两个独立服务永久同步作为目标。
- 内置实现完成前，本仓库原有 API/白名单/队列/回执约定仍适用于旧服务；只检查并处理影响现有运行的必要兼容问题。不能因方案确定就删除代码、停旧服务、撤销凭据或归档仓库。
- 迁移必须保留游标、去重、固定笔记/附件 ID、待处理任务和回执结果，已发送或不确定的回复不能重新发送。真实状态与凭据只经私有运维渠道转移，不复制进 Git。
- 修改另一项目之前读取其 AGENTS、检查工作区并保留已有改动；`~` 指当前用户主目录。迁移期任务与 PR 互引，详细实施以 Memos 主任务为准。

## 任务记录

- 实质需求、修复和接口适配使用 `docs/tasks/TEMPLATE.md` 创建或续接 `TASK-YYYYMMDD-短主题.md`；先记录目标与验收标准，收尾补充带日期的实现、实际验证和未覆盖范围。相关小修可共用记录。
- `docs/tasks/INDEX.md` 只保留编号、标题、关键词和详情链接；不维护依赖外部变化的 CI、合并、部署状态。已发生的操作可附日期/提交/证据，后续进展查关联 PR、Actions、Release 或私有运维记录。
- 助手维护本次记录和索引；不为追赶外部状态反复提交文档。继续旧任务时补充新事实并说明已解决的旧限制，不推测期间进展。
- 内置迁移的整体目标和后续功能 Task 由 Memos 维护；本仓库记录旧实现与必要移交事实，互引主任务和 PR，不复制两套长期计划。
- `docs/README.md` 提供按需导航；历史过多时按年份归档并保留索引入口。公开记录仅保留脱敏事实，不复制聊天、配置、日志或截图。文档修改执行暂存区及分支差异的空白检查，检查引用路径；纯文档修改不要求运行应用测试。
