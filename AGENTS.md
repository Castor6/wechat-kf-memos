# 项目约定

- 先读 README.md、docs/requirements.md、docs/memos-contract.md。当前 0.1.2，个人自用，定制 Memos 优先，不承诺通用兼容；历史阶段见 docs/development-status.md。
- 使用微信客服独立后台官方 API，不混用企业微信自建应用配置。
- 公开仓库只放代码、合成测试与脱敏文档。凭据、生产地址、用户标识、真实聊天、状态库、运维备份不得提交或打印。提交前遵循 docs/security.md。
- PRIVATE、本人白名单、默认个人空间；不自动信任第一位发送者。定制版标签独立保存，不回写正文。
- 仅通过 API 操作 Memos。媒体是一篇笔记及其附件；独立微信笔记不建占位。
- 保持游标/队列事务、固定来源 ID 幂等、回执独立持久化。先保存再成功回执；发送不确定时不能盲目重发。
- 附件仅由 Memos 长期保存；扩展缓存需明确清理、容量和失败恢复。
- 检查：uv run pytest -q；uv run ruff check .。真实 API 测试仅用本机临时实例，不访问生产。
- 不把模拟测试写成真机验收，不把 API 接受回执写成手机端已读。部署前备份配置、队列和旧安装包，真实部署资料留在私有运维目录。
